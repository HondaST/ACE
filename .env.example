# Copy this file to .env and fill in your values.
# .env is git-ignored and must never be committed.

# MS SQL Server connection
DB_SERVER=localhost\SQLEXPRESS
DB_DATABASE=tax-paladin
DB_ENCRYPT=false
DB_TRUST_SERVER_CERT=true

# Set DB_USER / DB_PASSWORD only if NOT using Windows Authentication.
# Leave both empty to use Windows Auth (Trusted Connection).
# DB_USER=
# DB_PASSWORD=
